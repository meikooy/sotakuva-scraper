if (process.env.NODE_ENV !== 'production') {
    require('dotenv').load();
}


/*
const client = algoliasearch(process.env.ALGOLIA_APPLICATION_ID, process.env.ALGOLIA_API_KEY);
const photosIndex = client.initIndex(process.env.INDEX_PHOTOS);
*/


